<UserControl x:Name="TaskInputControl" x:Class="LauncherZ.Controls.TaskInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="480">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="100*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="CtlHintText" Margin="14,10,10,0" TextWrapping="Wrap" Text="{Binding HintText, ElementName=TaskInputControl}" Foreground="{DynamicResource TitleBrush}" Grid.Row="0" FontSize="14"/>
        <TextBox x:Name="CtlTextBox" Padding="2,4" Margin="10,4,10,10" Text="{Binding Text, ElementName=TaskInputControl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsUndoEnabled="False" Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" Background="{DynamicResource InputBackground}" Foreground="{DynamicResource InputTextBrush}" CaretBrush="{DynamicResource InputTextBrush}" FontSize="16"/>

    </Grid>
</UserControl>
